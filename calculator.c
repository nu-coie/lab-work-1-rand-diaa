#include <stdio.h>
#include <cs50.h>


int main(void)
{
// Accept Input From user (char), where (a) stands for "Addition", (m) for "Multiplications," and (d) for "Division,"

char op = get_char("op:    ");

if (op == 'a' );

int x = get_int("X:    ");
int y = get_int("Y:     ");

int z = x + y;



